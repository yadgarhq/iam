# What this module vendors from yadgarhq/proto (D70). buf closes the import graph
# itself, so listing yadgar/iam/v1 also brings yadgar/common/v1.
#
# yadgar/iamdb/v1 is listed explicitly as well because this service is a CLIENT
# of IamDbService — it needs the service definition, not only the messages the
# API references, and an import closure brings the latter but not the former.
yadgar/iam/v1
yadgar/iamdb/v1
